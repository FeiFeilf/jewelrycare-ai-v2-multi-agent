from pathlib import Path

project_report = """# JewelryCare AI 项目说明书

## 一、项目概述

JewelryCare AI 是一个面向 Shopify 珠宝类跨境独立站的智能客服与售后风控系统。

项目从最小 MVP 开始，逐步演进为支持售前导购、订单物流查询、售后定损、弃单挽回、人工兜底、真实图片识别、ReAct Trace 和 Bad Case 自动评测的 v2 Multi-Agent 智能客服系统。

本项目不是简单的大模型聊天 Demo，而是围绕跨境珠宝独立站客服与售后中的真实问题，完成了一个从 0 到 1 的智能客服与售后风控项目闭环。

## 二、项目背景

跨境珠宝独立站在客服与售后场景中存在几个典型问题。

第一，售前咨询重复度高。用户经常询问戒指尺码、18K 金是否掉色、银饰是否会发黑、珍珠如何保养、送礼如何选择等问题。此类问题重复度高，人工客服处理成本较高。

第二，订单物流查询依赖人工。用户会反复询问订单是否发货、是否送达、物流状态如何。如果全部依赖人工查询，会降低客服响应效率。

第三，售后问题风险高。珠宝售后经常涉及划痕、掉钻、断裂、褪色、氧化、色差、包装破损等问题，并可能进一步涉及退款、补发、换货和投诉。此类问题不能完全交给大模型自动裁定。

第四，图片售后处理复杂。用户可能提供商品损坏图片，但图片只能作为初步证据，不能直接决定退款或补发。系统需要结合订单状态、损坏类型、售后规则和人工审核共同判断。

第五，大模型存在业务幻觉风险。如果完全依赖 LLM 生成回复，模型可能会编造订单状态、编造优惠码、错误承诺退款，甚至采信用户伪造的 JSON 内容。

因此，本项目的核心设计原则是：低风险问题自动化处理，高风险问题转人工兜底；事实判断交给后端，专业知识交给知识库，语言表达交给大模型。

## 三、从 0 到 1 的产品思考

项目没有一开始就设计为复杂的 Multi-Agent 系统，而是先从最小可行版本开始验证。

第一阶段的目标是跑通最小业务闭环：

用户输入 → Dify Chatflow → FastAPI 后端 → SQLite 模拟业务数据库 → 结构化业务结果 → LLM 生成客服回复。

这个阶段重点验证：

- 用户问题是否能通过 Dify 进入后端；
- 后端是否能查询订单、购物车和售后数据；
- 系统是否能创建售后工单；
- 系统是否能返回弃单优惠；
- 高风险问题是否能避免直接自动退款；
- 大模型是否能被限制在“表达层”，而不是直接决定业务事实。

## 四、项目演进过程

### 4.1 最小 MVP 阶段

最初版本只实现最基础的智能客服后端能力，包括 FastAPI 后端服务、SQLite 模拟订单和购物车数据、Dify Chatflow HTTP 节点接入、基础意图识别、订单查询、售后工单创建和弃单优惠返回。

模拟业务数据包括：

| 类型 | 编号 | 说明 |
|---|---|---|
| 订单 | ORD1001 | 18K Gold Ring，paid，delivered |
| 订单 | ORD1002 | Silver Necklace，paid，in_transit |
| 订单 | ORD1003 | Pearl Earrings，refunded，delivered |
| 购物车 | CART2001 | Rose Gold Bracelet，FREE-SHIPPING |
| 购物车 | CART2002 | Moissanite Ring，SAVE10 |

MVP 阶段验证了系统方向可行，但也暴露出问题：单一流程难以维护，售前、物流、售后、投诉、优惠逻辑混杂，缺少人工兜底记录，缺少可解释执行过程，图片售后无法稳定接入，也缺少系统化测试集。

### 4.2 业务能力补全阶段

MVP 跑通后，系统开始补全真实客服系统所需的业务闭环能力。

主要补充内容包括：

- 售后工单字段增强：工单不再只是记录一句话，而是包含订单号、问题类型、风险等级、证据要求、建议处理方式和处理状态。
- 人工兜底记录：对高风险售后、强投诉、退款争议、订单异常等场景创建人工兜底记录。
- 弃单挽回记录：记录用户是否触发购物车优惠提醒，避免系统随意编造优惠码。
- 运营统计接口：增加订单、工单、人工兜底、弃单挽回等运营统计接口。

这一阶段解决的问题是：系统不能只是回答用户，还要能沉淀真实业务动作。

### 4.3 Multi-Agent 架构升级阶段

随着业务场景增加，单一后端函数逐渐难以维护，因此项目升级为 Multi-Agent 架构。

| Agent | 职责 |
|---|---|
| Router Agent | 判断用户意图并路由到不同业务 Agent |
| Order Agent | 查询订单与物流状态 |
| After-sales Agent | 创建售后工单并判断风险等级 |
| Vision Agent | 根据图片摘要判断损坏类型与售后风险 |
| Shopify Agent | 模拟优惠码、退款审核、补发等电商后台动作 |
| Recovery Agent | 处理弃单挽回 |
| Memory Agent | 处理多轮对话中的用户偏好与上下文 |
| Handoff Agent | 创建人工兜底记录 |
| Task Agent | 创建后续跟进任务 |
| Evaluation Agent | 支持 Bad Case 自动评测 |

该阶段使系统从“一个接口处理所有事”升级为“多个 Agent 分工协作”。

### 4.4 ReAct Trace 可解释执行阶段

为了避免系统成为黑盒，本项目加入 ReAct Trace 机制。

ReAct Trace 的执行形式是：Thought → Action → Observation。

例如售后场景中，系统会记录：用户描述了商品问题，需要判断是否属于售后；调用 Order Agent 查询订单；查询到订单 ORD1001，商品已送达；调用 After-sales Agent 创建售后工单；高风险时调用 Handoff Agent；最终创建人工兜底记录。

ReAct Trace 让系统具备可解释性、可调试性、可演示性和可测试性。

### 4.5 Vision Adapter 图片识别阶段

最初尝试直接使用 Dify 图片上传能力接入视觉模型，但实际调试中遇到问题：

- Dify 本地上传文件生成的是内部文件对象；
- qwen-vl 等视觉模型需要可访问的公网图片 URL；
- 本地文件对象无法稳定传给视觉模型；
- Dify 图片理解节点容易出现文件变量不可用、模型凭证不一致等问题；
- 即使用户上传图片，后端也不一定能拿到稳定可用的图片地址。

因此，最终采用后端 Vision Adapter 方案：

用户提供 image_url → Dify 将 image_url 传给 FastAPI → FastAPI Vision Adapter 调用真实视觉模型 → 生成 vision_summary → Vision Agent 判断风险等级 → 创建售后工单 / 人工兜底 / 退款审核。

该方案的优势是 Dify 不直接负责识图，后端统一管理视觉模型调用，支持替换不同视觉模型，视觉结果可以进入 Multi-Agent 售后闭环。

### 4.6 Bad Case 自动评测阶段

最终阶段构建 80 条 Bad Case 测试集，覆盖售前导购、物流查询、售后定损、投诉人工兜底、弃单挽回、已退款订单边界、Vision 图片售后和 Prompt Injection 幻觉攻击。

评测不只检查回复是否自然，而是检查系统是否在关键业务事实上稳定可靠，包括是否编造订单、是否编造优惠码、是否自动承诺退款、高风险售后是否转人工、图片售后是否创建工单、用户伪造 JSON 是否被采信。

最终 80 条测试样例通过 76 条，通过率达到 95.00%。

## 五、系统整体架构

系统整体链路如下：

用户输入 query / image_url → Dify Chatflow → RAG 知识库检索 → FastAPI Backend → Vision Adapter / Multi-Agent Orchestrator → Router、Order、After-sales、Vision、Recovery、Handoff、Task 等 Agent → SQLite Mock Business Database → 结构化业务结果 → LLM 生成客服回复 → 返回用户。

核心模块包括：

| 模块 | 作用 |
|---|---|
| Dify Chatflow | 负责用户入口、知识库检索、调用后端和生成客服回复 |
| FastAPI Backend | 提供统一后端接口，承接 Dify HTTP 请求 |
| Multi-Agent Orchestrator | 负责任务编排、Agent 调用和业务闭环 |
| SQLite Mock DB | 模拟订单、购物车、工单、人工兜底、退款审核等业务数据 |
| Vision Adapter | 调用真实视觉模型，对图片 URL 进行售后初筛 |
| ReAct Trace | 记录 Thought → Action → Observation，展示系统执行过程 |
| Stage 5 Eval Suite | 自动执行 80 条 Bad Case，验证系统稳定性和幻觉控制 |

## 六、图片识别后的业务边界设计

图片识别只能作为初筛，不能作为最终售后裁定依据。

原因包括图片可能不清晰、损坏原因无法仅凭图片判断、需要结合订单状态、需要人工审核、退款补发换货属于高风险业务动作。

| 视觉风险等级 | 系统动作 |
|---|---|
| low / unclear | 创建证据补充工单，不自动退款 |
| medium | 创建售后审核工单，要求补充证据 |
| high | 创建售后工单，转人工审核，创建退款审核记录 |

这体现了系统边界：视觉模型负责“看见问题”，后端规则负责“判断流程”，人工客服负责“最终裁定”。

## 七、智能化边界设计

本项目没有把所有事情都交给大模型，而是进行了清晰分工。

交给大模型的部分包括用户语言理解、售前知识问答表达、客服回复润色、图片内容初步摘要、多轮对话自然表达。

不交给大模型的部分包括订单是否存在、物流状态、工单号、优惠码、是否已退款、是否创建人工兜底、是否创建退款审核记录。

整体设计可以概括为：事实判断后端化，专业知识 RAG 化，语言表达 LLM 化，高风险决策人工化。

## 八、项目关键难点与解决方案

| 难点 | 问题表现 | 解决方案 |
|---|---|---|
| Dify 无法通过 localhost 调后端 | Dify 在 Docker 内部，访问 localhost 指向容器自身 | 将后端加入 Dify Docker 网络，使用 customer-tools 容器名访问 |
| Dify 文件上传不稳定 | 上传文件对象无法稳定形成视觉模型可访问 URL | 改用 image_url，后端 Vision Adapter 统一调用视觉模型 |
| 视觉模型接口 404 | Workspace 专属地址或路径不匹配 | 切换 DashScope OpenAI-compatible 共享端点 |
| 识图成功但没进售后 | Router 只看文本，没有使用 vision_summary | 将 vision_summary 注入路由上下文，增加 Vision Fallback Ticket |
| Router 关键词过敏 | “掉色”误判售后，“物流一般多久”误判订单查询 | 增加业务路由提示和 Stage 5 后处理规则 |
| 高风险动作不能由模型决定 | 用户可能诱导模型直接退款或伪造订单 | 退款、工单、优惠、订单状态全部以后端为准 |

## 九、最终系统能力

最终版本支持：

1. 售前导购；
2. RAG 珠宝知识问答；
3. 订单物流查询；
4. 售后定损；
5. 真实视觉模型图片 URL 识别；
6. 售后工单创建；
7. 退款、补发、换货审核记录；
8. 弃单挽回；
9. 人工兜底；
10. ReAct Trace；
11. Memory Agent；
12. Shopify Mock 工具链；
13. Bad Case 自动评测；
14. Prompt Injection 风险控制。

## 十、项目价值总结

本项目的价值不在于简单调用大模型，而在于把大模型放入真实业务流程中，并解决智能客服落地时的关键问题：

- 从最小 MVP 开始验证业务闭环；
- 覆盖跨境珠宝独立站高频客服场景；
- 用后端结构化结果约束大模型幻觉；
- 用 Vision Adapter 接入真实图片识别能力；
- 用 Multi-Agent 拆分复杂业务流程；
- 用 ReAct Trace 提升系统可解释性；
- 用 80 条 Bad Case 自动评测验证稳定性；
- 用人工兜底机制控制退款、补发、投诉等高风险场景。

## 十一、后续扩展方向

后续可以接入真实 Shopify Admin API、真实物流查询接口、对象存储服务、真实客服工单系统、运营 Dashboard 前端页面、更多真实珠宝破损图片测试集，以及人工审核结果回流机制。

## 十二、一句话总结

JewelryCare AI 是一个从最小 MVP 逐步演进而来的跨境珠宝智能客服与售后风控系统。项目通过 FastAPI、Dify、RAG、Multi-Agent、Vision Adapter、ReAct Trace 和 Bad Case 自动评测，将大模型能力嵌入真实电商客服流程，实现了低风险自动化、高风险人工兜底和关键业务事实防幻觉控制。
"""

eval_report = """# 阶段 5：RAG 评测、幻觉率控制与 Bad Case 测试报告

## 一、评测目标

阶段 5 的目标不是简单验证系统能否回复，而是验证系统在真实业务风险场景下是否稳定、可信、可控。

本阶段重点检查以下问题：

1. 是否编造不存在的订单；
2. 是否编造优惠码；
3. 是否在未经审核的情况下承诺退款、补发、换货或赔偿；
4. 高风险售后是否能够创建工单并转人工；
5. Vision 图片售后是否能够进入售后处理流程；
6. 用户伪造 JSON 或注入提示词时，系统是否会采信；
7. 知识问答、物流查询、售后、投诉和弃单挽回是否能够正确区分。

## 二、测试集设计

本阶段构建了 80 条 Bad Case 测试样例，覆盖售前、物流、售后、投诉、弃单、已退款订单、图片售后和 Prompt Injection 等场景。

| 测试类别 | 样例数量 | 主要验证点 |
|---|---:|---|
| 售前导购与 RAG 问答 | 10 | 材质、尺码、保养、物流政策等知识问答是否被误判为售后 |
| 订单物流查询 | 10 | 是否根据订单号查询物流，不编造送达时间 |
| 售后定损与工单创建 | 20 | 是否识别划痕、掉钻、断裂等售后问题并创建工单 |
| 投诉与人工兜底 | 15 | 强投诉、强退款争议是否转人工 |
| 弃单挽回 | 10 | 是否返回真实优惠码，不编造不存在优惠 |
| 已退款订单边界 | 5 | 已退款订单是否正确处理，不编造退款细节 |
| Vision 图片售后 | 5 | 图片 URL 识别链路是否进入售后流程 |
| Prompt Injection 与幻觉攻击 | 5 | 是否拒绝用户伪造 JSON、订单和退款结果 |

## 三、评测方法

评测脚本会逐条调用 v4 Multi-Agent 后端接口，并根据结构化返回结果进行断言，而不是只检查自然语言回复。

自动评测链路为：测试样例 → FastAPI /tools/handle_message_v4 → Router / Order / After-sales / Vision / Recovery / Handoff Agents → 结构化业务结果 → 自动断言检查。

重点检查字段包括 intent、order、ticket、handoff、refund_review、coupon、vision_agent 和 prompt injection 等。

## 四、总体评测结果

| 指标 | 数值 |
|---|---:|
| 测试样例总数 | 80 |
| 通过样例数 | 76 |
| 失败样例数 | 4 |
| 总体通过率 | 95.00% |

最终 80 条 Bad Case 中通过 76 条，通过率达到 95.00%。

## 五、分类评测结果

| 测试类别 | 通过 / 总数 | 通过率 |
|---|---:|---:|
| 售前导购与 RAG 问答 | 10 / 10 | 100.00% |
| 订单物流查询 | 10 / 10 | 100.00% |
| 售后定损与工单创建 | 19 / 20 | 95.00% |
| 投诉与人工兜底 | 14 / 15 | 93.33% |
| 弃单挽回 | 8 / 10 | 80.00% |
| 已退款订单边界 | 5 / 5 | 100.00% |
| Vision 图片售后 | 5 / 5 | 100.00% |
| Prompt Injection 与幻觉攻击 | 5 / 5 | 100.00% |

## 六、关键结果分析

售前问答场景通过率为 100%。系统能够区分“18K 金是否掉色”“银饰是否发黑”“戒指尺寸如何选择”等知识类问题，不会将其误判为售后投诉或退款请求。

物流查询场景通过率为 100%。系统能够根据订单号查询模拟订单状态，并避免编造送达时间或物流细节。

售后场景通过率为 95%。系统能够对划痕、掉钻、断裂、褪色、氧化、色差等问题进行风险分级，并创建售后工单。高风险售后会触发人工审核。

投诉人工兜底场景通过率为 93.33%。系统能够识别投诉、强退款争议、强情绪表达，并避免直接承诺退款或赔偿。

弃单挽回场景通过率为 80%。系统能够识别有效购物车并返回对应优惠策略，同时避免编造不存在的优惠码。剩余失败样例主要属于边界表达问题。

Vision 图片售后场景通过率为 100%。说明图片 URL → Vision Adapter → vision_summary → Vision Agent → 售后工单的链路已经跑通。

Prompt Injection 风险测试通过率为 100%。系统不会采信用户伪造的订单 JSON、退款结果、优惠码或视觉摘要，关键业务事实仍以后端结构化结果为准。

## 七、未通过样例复盘

剩余未通过的 4 条样例主要集中在边界场景，包括部分售后与人工兜底边界、购物车不存在时的处理策略、购物车保留问题的意图识别等。

这些失败不属于严重业务风险，未出现以下高风险问题：

- 编造订单；
- 编造优惠码；
- 自动承诺退款；
- Prompt Injection 被采信；
- Vision 图片售后完全未处理。

## 八、结论

阶段 5 测试表明，JewelryCare AI v2 Multi-Agent 系统已经具备较好的业务稳定性和风控能力。

系统能够在售前、物流、售后、投诉、弃单、图片识别和 Prompt Injection 等关键场景中保持较高通过率，并通过后端结构化结果约束大模型幻觉。

最终 80 条 Bad Case 通过 76 条，通过率为 95.00%。该结果可以作为项目最终测试与交付依据。
"""

readme = """# JewelryCare AI v2：跨境珠宝智能客服与售后风控系统

JewelryCare AI 是一个面向 Shopify 珠宝类跨境独立站的智能客服与售后风控系统。

项目从最小 MVP 开始，逐步演进为支持售前导购、订单物流查询、售后定损、弃单挽回、人工兜底、图片识别、ReAct Trace 和 Bad Case 自动评测的 v2 Multi-Agent 闭环系统。

本项目不是简单的大模型聊天 Demo，而是围绕跨境电商客服与售后中的真实业务问题，构建了一个可运行、可解释、可测试、可扩展的智能客服后端系统。

## 项目文档

| 文档 | 说明 |
|---|---|
| [项目说明书](docs/PROJECT_REPORT.md) | 完整介绍项目背景、从 0 到 1 的演进过程、图片识别、智能化边界、难点与解决方案 |
| [阶段 5 中文评测报告](docs/eval_reports/stage5_eval_report_80cases_95pass.md) | 80 条 Bad Case 自动评测、幻觉率控制、分类通过率与复盘 |
| [阶段 5 原始 JSON 报告](docs/eval_reports/stage5_eval_report_80cases_95pass.json) | 自动评测脚本生成的结构化结果 |

## 核心亮点

- 从最小 MVP 逐步演进到 v2 Multi-Agent 闭环系统；
- 基于 FastAPI + Dify + SQLite 搭建可运行的客服后端；
- 引入 Router、Order、After-sales、Vision、Recovery、Handoff、Task 等 Agent；
- 通过 Vision Adapter 接入真实视觉模型，实现图片 URL 售后初筛；
- 使用 ReAct Trace 记录 Agent 执行过程，提升可解释性；
- 将订单、物流、工单、优惠码、退款审核等业务事实交给后端处理，降低大模型幻觉；
- 构建 80 条 Bad Case 测试集，最终通过率达到 95.00%。

## 系统架构

用户输入 query / image_url → Dify Chatflow → RAG 知识库检索 → FastAPI 后端 → v4 Multi-Agent Orchestrator → 业务 Agent 与工具 → 结构化业务结果 → LLM 生成客服回复 → 返回用户。

## 目录结构说明

| 路径 | 作用 |
|---|---|
| app/ | 后端核心代码目录，包含 FastAPI 服务、Agent 编排、数据库和业务逻辑 |
| app/agents/ | Multi-Agent 模块，每个 Agent 负责一个业务能力 |
| app/tools/ | 工具层，包括视觉模型适配器、Shopify Mock、Memory、Evaluation 等 |
| data/eval_cases/ | Stage 5 的 80 条 Bad Case 测试样例 |
| docs/ | 项目文档目录 |
| docs/eval_reports/ | 自动评测报告目录 |
| scripts/ | 测试脚本、检查脚本、文档生成脚本 |
| tests/ | 端到端测试脚本 |

## 最终评测结果

| 指标 | 数值 |
|---|---:|
| 测试样例总数 | 80 |
| 通过样例 | 76 |
| 失败样例 | 4 |
| 通过率 | 95.00% |

## 本地启动

```bash
docker compose up -d --build
curl http://127.0.0.1:8000/health
py
